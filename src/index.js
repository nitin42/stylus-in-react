const stylus = require('./main/createStylusComponent')
const keyframes = require('./parse/keyframes')
const fontFace = require('./parse/fontFace')

module.exports = {
  stylus,
  keyframes,
  fontFace
}
